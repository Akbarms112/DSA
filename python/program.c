#include<stdio.h>
#include<stdlib.h>

struct node
{
    int data;
    struct node* next;

};
struct node* create(struct node* head,int d)
{


}
int main()
{
    int n;
    printf("Enter the number of terms:");
    scanf("%d",&n);
    
}